import { API } from "../api/api";

const initState = {
  loading: true,
  accounts: [],
};

const accounts = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default accounts;
